import React from 'react';
import { StyledFooter } from '../utils/StyledComponents/Styled';

export default function Footer() {
  return (
    <StyledFooter>
          Ecommerce created during Wizeline’s Academy React Apprenticeship
    </StyledFooter>
  )
}
